server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;

    # Fallback for React Router (VERY IMPORTANT)
    location / {
        try_files $uri /index.html;
    }

    # Assets served directly
    location /assets/ {
        try_files $uri =404;
    }
}
